query GetDecks {
  getDecks {
    id
    name
    author {
      id
      username
    }
  }
}

query GetDeckByNameAndAuthor($name: String!, $author: String!) {
  getDeckByNameAndAuthor(name: $name, author: $author) {
    id
    name
    description
    visibility
  }
}

query GetFeed {
  feed {
    id
    description
    url
    postedBy {
      id
      username
    }
  }
}

query RefreshAccessToken {
  refreshAccessToken {
    accessToken
    user {
      id
      username
      email
    }
  }
}
